default:
  @just --list

dev:
  pnpm dev

test:
  pnpm test

lint:
  pnpm lint

typecheck:
  pnpm typecheck

build:
  pnpm build

fix:
  pnpm biome check --write .

clean:
  rm -rf node_modules .turbo dist
