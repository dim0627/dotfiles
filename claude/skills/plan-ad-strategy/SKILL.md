---
name: plan-ad-strategy
description: X(Twitter)広告クリエイティブの戦略を立案する。対象プロダクトの docs/product.md（コンセプト/ペルソナ/ポジショニング/ブランド）＋（あれば）広告運用レポートから、次に撃つクリエイティブの戦略候補を複数出す。各候補は「インサイト/訴求軸/メッセージ方向/クリエイティブ方向/表現モード（器の選択）/全スロットのコピー方向/検証仮説」を持つ。出力は人が読んで選べる Markdown の戦略まとめで、人間のデザイナーでも下流の制作工程でも受け取れる。taberu.pro / honn.me 等で使う。「広告戦略立てて」「次に何を訴求する」「訴求軸出して」で起動。
user-invocable: true
---

# plan-ad-strategy（広告戦略立案）

X(Twitter)広告クリエイティブの**戦略**を立てる。「どのインサイトに、どの訴求軸で、どんなメッセージを、どんなクリエイティブ方向で、どの器（表現モード）で当て、各スロットのコピーをどう振り、何を1変数として検証するか」を**複数候補**で出力する。

このスキルは**良い戦略候補を複数出すまで**が仕事。1つに絞らない（絞り込みは人の握り）。出力は**それ自体で完結する戦略まとめ**で、誰が受け取って制作してもよい（人間のデザイナー、画像生成、カンプ作成、いずれも一級の消費者）。**実際にどう制作するか（数値スペック・文字組み・絵そのもの）には踏み込まない**。

## 専門判断は ad-marketer に委譲する

広告マーケの分析フレーム（Who→What→How / JTBD / 機能・感情・社会の3層 / 進歩の4つの力 / ベネフィット訴求 / X広告のアナトミー / 戦略と制作の管轄線）の**正本は `ad-marketer` サブエージェント**が持つ。このスキルはそれを再定義しない。

**このスキルの役割は「立案という成果物がどんな形であるべきか」を定義すること**（下記「出力フォーマット」＝成果物契約）。実際の思考は `ad-marketer` に、下記の入力とこの出力フォーマットを渡して依頼する。ad-marketer は同じフレームで批評・数値解析もこなすが、このスキルが呼ぶのは**立案モード**。

## アウトプットの流れ

```
docs/product.md（＋あれば運用レポート・過去出稿）
      │
      ▼
[plan-ad-strategy] ── ad-marketer に立案を依頼し、戦略候補を複数出す（Markdown）
      │
      ▼
  人が握る ── どの戦略を今回撃つか選ぶ（自動確定しない）
      │
      ▼
   制作 ── 選ばれた戦略が要求するクリエイティブを作る
```

## 前提（満たさなければ止まる）

- **対象プロダクトの `docs/product.md` が存在すること**。無ければ「先に docs/product.md を整備してください」と伝えて停止する（ペルソナや訴求を憶測で捏造しない）。product.md に期待する情報（コンセプト/ペルソナ/ポジショニング/トーン&ボイス/ブランドDNA/ブランド方針）と、それが無い/薄いときの整備の手伝いは `ad-marketer` の管轄。

## 入力（ad-marketer に渡すもの）

- **`docs/product.md`（正本・必須）**: プロダクト知識の正本。コンセプト/ペルソナ/ポジショニング/トーン&ボイス/ブランド資産を読む。
- **広告運用レポート（あれば）**: X Ads Manager 等の実績（CTR / CVR・勝ち負けクリエイティブ）。あると検証仮説と次の打ち手の精度が上がる。**無くても動く**（出稿0回でも戦略は出せる。ただし検証仮説は当て込みになる点を添える）。
- **過去出稿の記録（あれば）**: Notion 広告レジストリ等。無ければスキップ。

> 運用レポートの有無をユーザーに対話で問い直さない。渡されていれば使う、無ければ product.md だけで進めて「検証仮説は実績でなく当て込み」と明示する。

## 出力

次に撃つクリエイティブの**戦略候補（複数）**を、人が読んで選べる **Markdown の戦略まとめ**で提示する（下記「出力フォーマット」）。クリエイティブ方向・コピーはすべて plain text の「方向」まで。数値スペック（hex・px・比率・座標）は含めない。

## 出力フォーマット（Markdown・複数候補）

戦略候補を**複数**、人が比較して選べる体裁で並べる。各候補は次の構造（※下記は構造を示すダミー。実値は product.md から）:

```markdown
## キャンペーンゴール
無料トライアル登録（product.md ／ ユーザー確認）

## ペルソナ・インサイト
（product.md のペルソナから。例: 始めたいが「続かない自分」を直視したくなくて踏み出せない）

---

## 候補A：〈訴求軸名〉
- **訴求軸**: 〈切り口〉（層: 感情）
- **JTBD**: [状況]のとき[動機]したい。なぜなら[成果]を得たいから
- **ベネフィット**: 機能でなく便益で1行
- **メッセージ方向**: 何を伝えるか
- **クリエイティブ方向**: 狙うムード ／ 主役と構図の方向 ／ do・don't ／ コピー用余白（※言葉のみ・数値なし）
- **表現モード**: 単発静止画 ／ 理由: 一撃で刺す単一メッセージで段階的な見せ方は不要だから
- **スロット別コピー方向**:
  - 投稿本文: 〈方向〉
  - メディア内: 〈主役メッセージの方向〉
  - （カード型なら）カード見出し: 〈方向〉（目安70字以内）
  - （カード型なら）CTA: 〈アクションの方向〉
- **検証仮説**: 何を変えると何が上がる＋なぜ
- **今回動かす変数**: 訴求軸

---

## 候補B：〈別の層の訴求軸〉
（同じ構造。機能/感情/社会を取りこぼさないよう層をまたぐ）

## 判断ポイント（あなたが握る）
- どの候補を今回撃つか（下記「判断ポイント」参照）
```

- 候補は**機能/感情/社会の3層を取りこぼさない**ように、層をまたいで複数出す。
- カルーセルを選んだ候補は、メディア内コピーを**パネル順の方向**で書く。
- クリエイティブ方向・コピーはすべて plain text の「方向」。**数値・hex・px・比率は持たせない**。

## 判断ポイント（人に握りを返す）

このスキルは戦略を**自動確定しない**。以下は機械的に決めず、候補を複数出してユーザーに選んでもらう:

- **どの候補を今回撃つか**＝3層（機能/感情/社会）のどの軸から攻めるかの最終選択（1クリエイティブ=1軸に絞る判断）。
- **低トラフィックで統計的有意に達しないとき**の暫定判断（必要配信量・打ち切り基準）。
- **Anxiety/Habit を下げる具体表現**（社会的証明・返金保証・移行コスト訴求等）の採否。
